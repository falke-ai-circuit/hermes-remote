name: hermes-remote
version: 0.1.0
kind: standalone
description: "Remote agent plugin for Hermes ecosystem. Registers tools to interact with remote agents via the hermes-remote server (list agents, execute shell commands, read/write files, capture screenshots)."

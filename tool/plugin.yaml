name: probe
version: 0.1.0
kind: standalone
description: "Remote agent plugin for Hermes ecosystem. Registers tools to interact with remote agents via the PROBE server (list agents, execute commands, read/write files, capture displays)."
